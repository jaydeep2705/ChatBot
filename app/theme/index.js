import Colors from './Colors';
import {
  Metrics,
  horizontalScale,
  moderateScale,
  verticalScale,
} from './Metrics';
import {Images, Icons} from '../assets';

export {
  Colors,
  Images,
  Icons,
  Metrics,
  horizontalScale,
  moderateScale,
  verticalScale,
};
